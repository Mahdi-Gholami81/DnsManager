# Superpowers agent scratch (ledger, briefs, review packages)
.superpowers/

# Planning notes (spec/plan docs) — local only
docs/

# Local runtime state — created automatically next to the script on first run
servers.ini

# Local release dry-run artifacts (real releases are built by CI)
stage/
stage-test/
DnsManager-v*.zip
*-test.zip

# Windows / editor noise
Thumbs.db
desktop.ini
.vscode/
*.swp
